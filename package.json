{
  "name": "interview-platform",
  "private": true,
  "scripts": {
    "build": "npm install --prefix frontend && npm install --prefix backend && npm run build --prefix frontend && rm -rf backend/dist && mkdir backend/dist && cp -r frontend/dist/* backend/dist",
    "start": "npm start --prefix backend"
  }
}